[package]
name = "setup-solarfiles"
version = "0.1.0"
edition = "2024"

[dependencies]
colored = "2"
dvs-interactive-cli-rs = { git = "https://codeberg.org/dvs/dvs-interactive-cli-rs.git", branch = "main" }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true